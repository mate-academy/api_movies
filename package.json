{
  "name": "api_movies",
  "version": "1.0.0",
  "description": "1. Fork this repo, clone the fork, and initialize a Node app in the folder. Add `node_modules` to `.gitignore`. Install `express` and `pg` to the folder.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GitKurmax/api_movies.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/GitKurmax/api_movies/issues"
  },
  "homepage": "https://github.com/GitKurmax/api_movies#readme",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^1.19.1",
    "pg": "^7.11.0"
  }
}
